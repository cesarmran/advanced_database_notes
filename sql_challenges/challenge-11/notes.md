En este challenge practiqué cómo se manejan los cambios en una base de datos usando migraciones y ORM entendí que no basta con cambiar el modelo sino que también se debe generar y aplicar una migración para que la base realmente se actualice también vi que las relaciones entre tablas ayudan a organizar mejor la información y que el rollback sirve cuando un cambio salió mal aunque hay que tener cuidado porque se pueden perder datos